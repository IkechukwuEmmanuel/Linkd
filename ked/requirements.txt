# Aggregate of all runtime dependencies (API + worker), kept so existing tooling
# and CI that runs `pip install -r requirements.txt` continues to work.
#
# For lean, per-process installs use the split files instead:
#   requirements-common.txt  — shared by API and worker
#   requirements-api.txt     — API process only
#   requirements-worker.txt  — Celery worker only
#   requirements-dev.txt     — everything + pytest/alembic (tests/CI)
#
# For a fully version-pinned (transitive) install, use requirements.lock.
-r requirements-api.txt
-r requirements-worker.txt
