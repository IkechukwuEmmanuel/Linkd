# Celery worker process. Install with:  pip install -r requirements-worker.txt
#
# Adds the heavy, worker-only enrichment stack (browser automation + scrapers +
# async HTTP) and Flower monitoring on top of the shared deps. The API image
# does NOT install these.
-r requirements-common.txt

flower==2.0.1
beautifulsoup4==4.12.2
selenium==4.15.0
playwright==1.40.0
aiohttp==3.9.1
